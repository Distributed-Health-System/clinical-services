# core-services
Monorepo containing the patient, doctor, appointment, telemedicine, and notification services. Each service is an independent microservice with its own database and Dockerfile and they communicate between each other using REST. They are grouped here to reduce context switching during development.
